# File Telemetry
The observability of file that should be processed to data is not optional but necessary, this repo aims to observe the file lifecycle, trace and what cloud be in distributed system.  

## TODO
1. User should register file telemetry service
- user should send user key(id) or request the key to file telemetry and file telemetry can provide user the user session key (uuid)
- file telemetry create subscription object and manage the subscription

